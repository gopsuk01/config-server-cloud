server:
  port: 9196

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/gopsuk01/cloud-config-server

eureka:
  client:
    service-url:
      defaultZone: "http://localhost:8761/eureka/"







